<%- include('partials/header') %>

    <main style="padding: 150px 5%; text-align: center; min-height: 60vh;">
        <h1 style="font-size: 3rem; color: var(--primary); margin-bottom: 1rem;">Oops!</h1>
        <p style="font-size: 1.2rem; color: var(--text-dim); margin-bottom: 2rem;">
            <%= message %>
        </p>
        <a href="/"
            style="background: var(--primary); color: white; padding: 12px 30px; border-radius: 30px; text-decoration: none; font-weight: bold; transition: background 0.3s;">
            Kembali ke Beranda
        </a>
    </main>

    <%- include('partials/footer') %>